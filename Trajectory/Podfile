# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Trajectory' do
  use_frameworks!

  # Pods for Trajectory
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

  pod 'GoogleSignIn'

  pod 'JTAppleCalendar', '~> 7.0'
  
  pod 'CodableFirebase', :git => 'https://github.com/alickbass/CodableFirebase.git'

  target 'TrajectoryTests' do
    inherit! :complete
    # Pods for testing
  end

  target 'TrajectoryUITests' do
    inherit! :complete
    # Pods for testing
  end

end
