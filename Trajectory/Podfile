# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'Trajectory' do
  use_frameworks!
  
  

  # Pods for Trajectory
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'FirebaseUI/Auth', '~> 4.0'
  pod 'FirebaseUI/Google'
  pod 'FirebaseUI/Facebook'

  pod 'GoogleSignIn'

  pod 'JTAppleCalendar', '~> 7.0'
  
  pod 'CodableFirebase'

  target 'TrajectoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TrajectoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
